<Window x:Class="ColorPicker.ZoomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Width="50" Height="50">
    <Border Background="Transparent" BorderBrush="#777" BorderThickness="0.7" CornerRadius="5">
        <Grid>
            <Rectangle x:Name="ZoomRectangle" RadiusX="5" RadiusY="5">
                <Rectangle.Fill>
                    <ImageBrush x:Name="ZoomImageBrush"/>
                </Rectangle.Fill>
            </Rectangle>
        </Grid>
    </Border>
</Window>
